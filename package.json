{
  "name": "turbun",
  "module": "index.tsx",
  "workspaces": [
    "src/apps**"
  ],
  "type": "module",
  "scripts": {
    "start": "lerna exec --parallel --scope nextjs -- bun start & lerna exec --parallel --scope sveltejs -- bun dev & lerna exec --parallel --scope astrojs -- bun dev & lerna exec --parallel --scope nuxtjs -- bun dev"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "lerna": "^7.3.0"
  }
}